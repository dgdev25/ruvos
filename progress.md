# Progress

## Parked

- Skills pack versioning, verification, and explicit update flow.
- Rationale: useful for distribution hygiene, but not critical path work right now.

## Resume later

- Verify bundled `skills.redb` integrity at startup.
- Add `ruvos skills pack update`.
- Surface pack drift in `doctor`.
- Add explicit pack version/hash metadata.

## Done

- Agentic evaluation harness (`ruvos eval <suite>` with `--write`/`--compare-to`).
- Orchestration handoff benchmark: GOAP pipeline correctness + conditional-edge graph routing.
- Swarm recovery benchmark: stale detection, failure/success policy, topology convergence.
- Skill-routing benchmark: query keyword hints per archetype + optional pack-based selection.
- Swarm learning-loop benchmark: 5 convergence scenarios (mesh, hierarchical, failure guard, mixed, history fallback).
