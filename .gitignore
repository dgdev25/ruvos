# Rust
/target/
Cargo.lock
*.rs.bk
**/*.rs.bk

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
*.o
*.a
*.so
*.dylib
*.rlib

# Test artifacts
*.test

# Temporary files
*.tmp
.env.local

# rUvOS runtime data + per-install signing key (never commit)
.ruvos/

# rUvOS generated agent artifacts (per-run skill selections + scaffolds) — never commit
crates/ruvos-mcp/generated/

# Superpowers process/planning docs — internal artifacts, not part of the repo
docs/superpowers/
README.old.md
assets/preview.html
